class Voter {

    _voted = '';
    _votedIndex = '';
    _weight = '';

    constructor(voted, votedIndex, weight) {
        this._voted = voted;
        this._votedIndex = votedIndex;
        this._weight = weight;
    }


}
